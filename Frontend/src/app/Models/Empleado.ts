// Define una interfaz para representar un empleado
export interface Empleado {
  idP: number; // Identificador único del empleado
  nombreP: string; // Nombre del empleado
  apellidoP: string; // Apellido del empleado
  correoP: string; // Correo electrónico del empleado
  sueldoP: number; // Sueldo del empleado
  fechaContratoP: Date; // Fecha del contrato del empleado
}
